{
  "schema_version": 1,
  "task_id": "V0.3-12-A",
  "title": "Desktop Runtime project read-model client",
  "objective": "Implement inspectProject(projectPath) in the desktop Runtime HTTP boundary: it calls GET /project/inspect?project_path=... and returns a typed Desktop read model { projectId, displayName, schemaVersion, createdAt, updatedAt }, mapping the Runtime's snake_case JSON onto camelCase only inside this boundary. projectPath is an explicit parameter on every call; there is no global current project, and the path is never normalised, resolved, lower-cased or rewritten - it is encoded as the project_path query parameter with URLSearchParams (or an equivalent safe query encoding) and forwarded verbatim. A successful response is validated field by field and a fresh object is built from exactly the declared fields - never cast. Failures are distinguished into three typed classes: a structured Runtime API error (the shared five-field envelope code / message / details / recoverable / source is preserved intact), a transport error, and a contract error (a 2xx payload the contract does not describe). Desktop-authored transport and contract diagnostics never add or echo projectPath, the request URL or the raw response body; a structured Runtime API failure preserves the Runtime-provided five-field envelope unchanged, so its server-provided message and details are authoritative Runtime diagnostics that the Desktop client does not re-sanitise.",
  "scope": "The new desktop Runtime project client module and its vitest suite.",
  "non_goals": [
    "Re-implementing the Runtime project domain - ProjectService stays the only project authority",
    "Changing the Runtime HTTP contract or any module under runtime/",
    "Any UI, project picker, recent-projects list or global current-project state",
    "Any Rust, Tauri IPC or orchestration change"
  ],
  "base_sha": "8522c0f2d33391a2d2092e648c446f5813187e98",
  "execution_mode": "native-shared",
  "branch": null,
  "worktree": null,
  "integration_paths": [],
  "owner": "sub-a",
  "allowed_paths": [
    "apps/desktop/src/runtime/project-client.ts",
    "apps/desktop/src/runtime/project-client.test.ts"
  ],
  "forbidden_paths": [
    "apps/desktop/src-tauri/**",
    "runtime/**",
    ".agent/**"
  ],
  "dependencies": [],
  "shared_contracts": [
    {
      "path": "runtime/canx/api/project.py",
      "sha": "451352faad16e44c36a9bebae3ab1ab355746aa5"
    }
  ],
  "acceptance_criteria": [
    "inspectProject(projectPath) requests GET /project/inspect with project_path encoded by URLSearchParams, and a non-empty path reaches the query string byte for byte",
    "A well-formed success payload is projected onto { projectId, displayName, schemaVersion, createdAt, updatedAt } with no extra field and no cast",
    "A malformed success payload - missing field, wrong field type, non-object, array - is refused as a contract error and produces no read model",
    "A structured Runtime failure keeps code / message / details / recoverable / source intact and is a Runtime API error, not a transport error",
    "A transport failure and an uninterpretable failure response are transport errors",
    "Desktop-authored transport and contract diagnostics add neither projectPath, the request URL nor the raw response body; a structured Runtime API failure preserves the Runtime-provided five-field envelope unchanged, so its message and details - which may legitimately carry a Runtime-provided path - are authoritative Runtime diagnostics that the Desktop client does not re-sanitise"
  ],
  "required_tests": [
    "frontend-test",
    "frontend-typecheck",
    "frontend-lint"
  ],
  "handoff_requirements": [
    "Report the exact command and result for pnpm test, pnpm typecheck and pnpm lint at the repository root",
    "Name every new test case and state the RED observation that preceded each new behaviour",
    "State explicitly that no Rust file, no Runtime file and no orchestration file was touched"
  ],
  "risk_class": "LOW",
  "status": "HANDOFF_READY",
  "revision": 3
}
