# Produced handoffs land here as <task-id>.json and <task-id>.md.
#
# Handoffs are evidence and are kept: a cancelled or failed task keeps whatever
# partial handoff it produced (AGENT-01 protocol §13). Nothing in this directory
# is executed - a handoff is data.
