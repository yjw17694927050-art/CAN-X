"""Engineering tooling for CAN-X.

This package holds the repository's development-time tooling. It is *not* part of
the shipped Runtime: nothing here is imported by ``canx`` and nothing here is
packaged into the wheel (``pyproject.toml`` packages only ``runtime/canx``).
"""
