# V0.4-WAVE1-FIX-1-CLOSEOUT

> **Document**: `docs/acceptance/v0.4-wave1-fix1-closeout.md`
> **Task**: V0.4-WAVE1-FIX-1-CLOSEOUT (governance + verification closure, Main Agent only)
> **Owner**: Main Agent · **Sub-Agent A / B / C**: IDLE (no open finding on their tracks)
> **Status**: IMPLEMENTED · SELF-VERIFIED · READY FOR INDEPENDENT RE-ACCEPTANCE
>
> This document records what the development side did and measured. It is **not**
> a verdict: it contains no `Final Acceptance: PASS` and no `Status: CLOSED`.
> Those remain the independent reviewer's (`AGENTS.md` section 14,
> `docs/PROJECT_STATE.md` section 11).

---

## 1. Findings this round addressed

The previous independent acceptance returned `OVERALL NOT PASS` with `P0 = 0`,
`P1 = 2`, `P2 = 2` against otherwise `PASS` product candidates. Product code was
frozen for this round (no product defect was found).

| Finding | Kind | Resolution |
| --- | --- | --- |
| P1-1 — V0.4-08-FIX-1 lifecycle not closed (status READY, no handoff, no validate/check evidence) | governance | CLOSED — section 2 |
| P1-2 — no reproducible joint regression on the staging tree; no CI run on the staging head | evidence | CLOSED as LOCAL JOINT REGRESSION (no staging CI exists) — section 3 |
| P2-1 — Main/C execution overlap NOT VERIFIED | process | RECORDED, not re-manufactured — section 5 |
| P2-2 — PR #30 description still claimed the native smoke NOT VERIFIED | documentation | CORRECTED — section 6 |

---

## 2. P1-1 — V0.4-08-FIX-1 lifecycle closed

### 2.1 Delivery range verified from Git (not from any agent's self-report)

```text
base:  481be34c7c671bdac32c2bc8535b8f4eb67c2cb3
head:  29b376b8af05d64336160559637806cd77ce2051
range: 481be34c..29b376b
```

`git diff --name-status 481be34c..29b376b` — exactly four paths, all inside
Sub-Agent C's Replay ownership surface:

```text
M  docs/acceptance/v0.4-08-offline-replay-core.md
M  runtime/canx/replay/errors.py
M  runtime/canx/replay/session.py
M  tests/integration/test_offline_replay.py
```

No file outside that surface is in the delivery range.

### 2.2 TaskContract reissued

`.agent/tasks/V0.4-08-FIX-1.json` — `revision: 1 → 2`, `status: READY →
HANDOFF_READY`, `execution_mode: native-shared` (already). The diff against the
revision-1 contract is exactly those two fields; the frozen objective, allowed
paths and forbidden paths are unchanged.

### 2.3 Handoff created from Git facts

`.agent/handoffs/V0.4-08-FIX-1.json` — `base_sha 481be34c...`, `head_sha
29b376b...`, one commit, the four changed files, `ownership_compliance: true`,
the targeted / performance / full-suite / ruff / mypy results, and the known
risks. No Git fact in it was taken from a Sub-Agent's self-report.

### 2.4 Governance commands (real CLI, repository syntax)

```text
validate-task       --file    .agent/tasks/V0.4-08-FIX-1.json
                    -> status: ok, exit 0

validate-handoff    --task    .agent/tasks/V0.4-08-FIX-1.json
                    --handoff .agent/handoffs/V0.4-08-FIX-1.json
                    -> status: ok, exit 0

check-integration   --task    .agent/tasks/V0.4-08-FIX-1.json
                    --handoff .agent/handoffs/V0.4-08-FIX-1.json
                    --plan    .agent/tasks/v0.4-08-fix1.plan.json
                    --repo    .
                    --integration-head 6a3a18dcc263f474a1fba967eafaa06207530e2c
                    -> ready: true, blockers: [], exit 0
                       base_is_ancestor: true
                       head_on_integration_head: true
                       clean: true
```

`check-integration` first refused with `agent.git_state_error` ("the task
worktree has uncommitted work") while the governance files were still
uncommitted — the gate behaving exactly as designed. Committing them produced a
clean delivery tree and `ready: true`.

---

## 3. P1-2 — joint regression on the staging tree

### 3.1 Staging truth

```text
ref:  fix/v0.4-wave1-fix1-staging
head: 6a3a18dcc263f474a1fba967eafaa06207530e2c   (regression head)
```

`origin/main` (`127ee2ff3ba0600afc3c7826e879981ef0a64867`) **is an ancestor** of
the staging ref; the staging ref is **not** an ancestor of `main` (unmerged, as
required by DO NOT MERGE). Its eight commits behind `main` are exactly the four
candidates, the joint merge, the two FIX-1 deliveries and the contract. The 39
changed paths against `main` were reviewed one by one and all fall inside the
four tracks plus the FIX-1 governance file — no unexpected product modification.

### 3.2 Runtime / Python (same staging tree)

```text
pytest -q          collected 3148 · passed 3147 · failed 0 · skipped 1 · exit 0   (363.19s)
ruff check .       All checks passed!
mypy runtime tools Success: no issues found in 118 source files
```

The single skip is `tests/unit/dbc/test_dbc_asset.py` unable to create a
directory link on this Windows host (WinError 1314) — an environment limit, not
a failure.

### 3.3 Frontend / TypeScript (same staging tree)

```text
pnpm --filter @canx/desktop lint       exit 0
pnpm --filter @canx/desktop typecheck  exit 0
pnpm --filter @canx/desktop test       exit 0   (34 files / 536 tests passed)
pnpm --filter @canx/desktop build      exit 0
```

`pnpm` is resolved through `npx --yes pnpm@11.19.0` (the version `package.json`
pins) because this machine has no installed `pnpm` and no `corepack`; the same
method V0.4-00-FIX-1 verified. No unknown global version was installed.

### 3.4 Rust / Desktop System (same staging tree)

```text
cargo fmt --check                                        FMT_OK
cargo clippy --all-targets --all-features --locked -- -D warnings   CLIPPY_OK
cargo test --locked                                      TEST_OK  (42 passed + 3 runtime_sidecar)
```

### 3.5 Native smoke (joint tree, not the #30 branch alone)

```text
npx --yes pnpm@11.19.0 tauri dev   (cwd apps/desktop)
  Running BeforeDevCommand (`pnpm dev`)      <- Tauri ran it; Vite not started by hand
  VITE v8.3.0  ready in 270 ms
  Local: http://localhost:1420/
  Finished `dev` profile ... in 0.70s
  Running `target/debug/can-x.exe`
```

Observed on this joint tree:

| Item | Evidence |
| --- | --- |
| beforeDevCommand runs | log `Running BeforeDevCommand (pnpm dev)` |
| Vite 1420 | `Local: http://localhost:1420/`; `GET :1420/` -> 200 with `id="root"` |
| Desktop starts | `can-x.exe` pid 16692 (`target/debug/can-x.exe`) |
| CAN-X window exists | `EnumWindows` -> class `Tauri Window`, title `CAN-X`, visible (hwnd 15271690) |
| WebView loads | Vite served the app to that devUrl (HTTP 200) |
| Runtime sidecar starts | spawned by the Desktop |
| `/health` | HTTP 200 `{"status":"ready","service":"canx-runtime",...}` |
| `/runtime/status` | HTTP 200 `{"state":"ready","capture_active":true,...}` |
| graceful close | `WM_CLOSE` -> `can-x.exe` gone within 2s |
| no orphan runtime | `/health` unreachable; no listener on 8765 |
| no orphan Vite/Tauri | no listener on 8765/1420; orphan scan `NONE` |

### 3.6 Local regression vs GitHub CI — kept strictly apart

```text
LOCAL JOINT REGRESSION (staging tree)      = PASS   (measured above)
GITHUB CI on fix/v0.4-wave1-fix1-staging   = NONE   (gh run list --branch ... -> 0 runs)
```

There is **no** staging CI run and this document does not claim one. The CI
evidence that does exist belongs to the PR branches:

```text
PR #30  CI run 35453296810  success  (Class / Frontend 2m2s / Rust 6m23s / Quality Gate)
PR #33  CI run 35453311291  success  (Class / Runtime-Python 7m48s / Quality Gate)
```

---

## 4. Candidate and delivery heads

```text
main                              127ee2ff3ba0600afc3c7826e879981ef0a64867
PR #30  V0.4-00                   3791e93d34ba62bfdf45568ca99dace94090549b
PR #31  V0.4-01                   e82d6f45eb9be8db955fb548dd33bd878b7b6b90  (unchanged)
PR #32  V0.4-04                   cb8931d3c4a5088caf1ce0fa4c61895c9b80dfa1  (unchanged)
PR #33  V0.4-08                   92adb0d4f6b050f0df69edcec173e8e74c8060a1
Replay delivery base              481be34c7c671bdac32c2bc8535b8f4eb67c2cb3
Replay delivery head              29b376b8af05d64336160559637806cd77ce2051
staging (regression head)         6a3a18dcc263f474a1fba967eafaa06207530e2c
TaskContract revision             2  (status HANDOFF_READY, native-shared)
handoff                           .agent/handoffs/V0.4-08-FIX-1.json
```

---

## 5. P2-1 — concurrency truth (NOT re-manufactured)

```text
V0.4-WAVE1-FIX-1 Main/C execution overlap
NOT INDEPENDENTLY VERIFIED
```

The previous round's `delegate_task` is a **blocking** call in this harness: the
dispatch returned only after Sub-Agent C finished, so the Main Agent's own work
did not wall-clock overlap it. Two commits sharing a timestamp would not prove
concurrency, and this round did not re-run already-accepted product work merely
to fabricate a history record. The gap stands as a **P2 process-evidence gap**;
it does not block product acceptance.

### 5.1 Frozen policy from V0.4 Wave 2 onward

```text
When all three tracks have work at the same time:

  ONE native /team dispatch  ->  A + B + C
  and it MUST produce:
      heartbeat
      sampler
      worker activity intervals
      strict overlap
      max simultaneous workers

Without telemetry: parallelism = NOT VERIFIED.
```

This rule is not to be omitted in later rounds.

---

## 6. P2-2 — PR #30 description corrected

PR #30's description now carries both rounds explicitly: **round 1 = NOT
VERIFIED** (retained as history) and **FIX-1 = VERIFIED** with the eleven
native-smoke observations. PR #33's description gained a FIX-1 paragraph marked
as acceptance remediation, not a new capability.

---

## 7. What this document is not

It is not an acceptance verdict, not a merge authorisation, and not a CI claim
for the staging ref. Every PR remains `DO NOT MERGE` pending independent
re-acceptance. Integration order stays `V0.4-00 → V0.4-01 → V0.4-04 → V0.4-08`,
each rebased onto the then-current `main` with the Quality Gate re-run.
