# V0.4-01 — Recorder Control Plane & Session API

> **Task**: V0.4-01 · **Owner**: sub-a
> **Branch**: `agent/V0.4-01-recorder-control-plane`
> **Base SHA**: `127ee2ff3ba0600afc3c7826e879981ef0a64867`
> **Status**: HANDOFF_READY — implementation complete and independently re-verified

## Provenance

The Sub-Agent wrote the implementation and its targeted tests (a real RED of
28 failed / 1 collection error -> GREEN) but its turn budget ended before the
mandatory full `pytest -q`, `mypy`, a clean `ruff` re-run, this document and the
commit. The Main Agent re-ran every outstanding gate below in a clean checkout of
this worktree and completed the delivery. Every number in this record is a Main
Agent re-run, not the Sub-Agent's self-report.

## What was built

- `runtime/canx/recorder/control.py` (new) — a typed Recorder observability model.
- `runtime/canx/api/recorder.py` (new) — the Recorder HTTP surface.
- `runtime/canx/api/app.py` — router mount + typed error wiring only (+23 lines).
- `runtime/canx/recorder/project_recorder.py` (+29), `runtime/canx/runtime/service.py`
  (+50) — observability seams only; capture ordering, the broker, cleanup and the
  existing stop semantics are unchanged.
- Tests: `tests/unit/recorder/test_recorder_control.py`,
  `tests/unit/api/test_recorder_api.py`,
  `tests/integration/test_recorder_control_plane.py`.

No second recorder start/stop authority was added: the Capture Runtime still owns
recording ingress lifecycle. Storage logic was not duplicated — DataSessionService,
DataSessionWriter and ProjectService remain the authority.

## Verification (Main Agent re-run, inside the worktree)

| Gate | Command | Result |
| --- | --- | --- |
| targeted | `pytest tests/unit/recorder tests/unit/api/test_recorder_api.py tests/integration/test_recorder_control_plane.py -q` | 67 passed |
| full suite | `pytest -q` | 2931 passed, 8 skipped, exit 0 |
| lint | `ruff check .` | All checks passed |
| types | `mypy runtime tools` | Success, no issues in 105 source files |

## Known gaps

- The Sub-Agent did not run the full suite itself; the Main Agent did (see Provenance).
- `tests/integration/test_packaged_runtime_smoke.py` skips here because the packaged
  `canx-runtime.exe` is not built in a worktree — environment, not a failure.
- No packaged-runtime recorder smoke was run (no packaged sidecar in this environment).
