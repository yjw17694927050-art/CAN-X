# V0.3-11 — Project Runtime Read Model API Foundation

> **Phase**: V0.3-11
> **Baseline commit**: `575dfcb` (on top of `2381628` — V0.3-10 delivery-gate advisory triage)
> **Date**: 2026-09-17
> **Report author**: development agent (self-verification only)
> **Acceptance state**: **Awaiting independent acceptance**

This report is the acceptance *evidence* for V0.3-11, per `docs/acceptance/README.md`. It
records what was implemented, what was measured, and what was **not** verified. It does not
grant the phase a verdict — the project owner / independent reviewer does.

---

## 1. Phase objective

Give the Runtime a formal, typed, **read-only** Project inspection / read-model API, so no
future Desktop has to ask a DBC endpoint whether a directory is a valid CAN-X project.

```text
caller supplies explicit project_path
        ↓
GET /project/inspect
        ↓
FastAPI adapter (canx/api/project.py)
        ↓
ProjectService.open          ← the project domain stays the authority
        ↓
validated CAN-X project (project.json + project.db + schema + agreement)
        ↓
ProjectHandle / ProjectMetadata
        ↓
temporary handle closed
        ↓
typed, immutable read model
```

Not in scope, and not built: a current project, a project picker, project create / delete /
rename / repair / upgrade, recent-project history, any Desktop work, any DBC change.

## 2. Scope implemented

- **One new Runtime endpoint**: `GET /project/inspect?project_path=<path>`.
- **One new response model**: `ProjectInspectResponse` — `project_id`, `display_name`,
  `schema_version`, `created_at`, `updated_at`, and nothing else.
- **A new router module** `runtime/canx/api/project.py` (188 lines, of which most is the
  reasoning for the four load-bearing properties), mounted from the application factory
  alongside the Trace and DBC routers.
- **Request-scoped handle lifecycle**: the project is opened, read and closed *inside one
  request*; the handle is taken as a context manager, so "opened" and "closed" cannot be
  separated by an early return.
- **Blocking work off the event loop**: the whole open → read → close sequence runs under
  `asyncio.to_thread`.

### 2.1 The exact contract

```text
GET /project/inspect
Query:    project_path: string            (required; passed through unmodified)
200:
{
  "project_id":     "2f1c3d4e-5a6b-4c7d-8e9f-0a1b2c3d4e5f",
  "display_name":   "Powertrain Rig",
  "schema_version": 1,
  "created_at":     "2026-09-17T16:40:12.345678+00:00",
  "updated_at":     "2026-09-17T16:40:12.345678+00:00"
}
400:  any typed `project.*` domain failure, in the shared five-field envelope
422:  api.request_validation_failed  (missing `project_path`)
```

`project_id` comes from the validated project identity (manifest and database agreement);
`display_name` / `created_at` / `updated_at` come from the canonical metadata row;
`schema_version` comes from the canonical manifest. None of them is derived from a file name
or a directory name.

## 3. Scope explicitly deferred

Unchanged from the phase brief: project create / delete / rename APIs, display-name change,
migration / repair / upgrade endpoints, set-current-project, close-current-project, a
recent-project list, project history, a Desktop project picker, an Open Project button,
native directory dialogs, a Tauri directory bridge, Desktop project state, any Zustand
project store, and DBC Workspace ↔ project wiring.

`ProjectService.create()` already exists in the domain and was deliberately **not** exposed.

## 4. Architecture / boundary statement

```text
UI  ≠  CAN Runtime  ≠  Agent Runtime  ≠  Storage
```

- **The project domain remains the authority.** The adapter never reads `project.json`,
  never opens `project.db`, never runs a SQL query of its own and never decides for itself
  what a valid project is. `ProjectService.open` performs path → manifest → database →
  schema → identity-agreement validation, and this module only projects its result.
  There is no second project parser to drift from the first.
- **No global current project.** The endpoint is stateless: every request supplies its own
  `project_path`, opens its own handle and closes it before answering. No
  `current_project`, no `active_project`, no module-level `ProjectHandle`, no cached handle,
  no session state.
- **The response is not a filesystem authority.** The caller's path, the manifest path, the
  database path and every internal directory (`dbc/`, `data/`, `cache/`, `logs/`, …) are
  absent from the projection.
- **`project_path` is not normalized.** It reaches `Path(...)` exactly as the caller wrote
  it: no `resolve()`, no `expanduser()`, no `lower()`, no separator rewriting, and no
  hand-written cross-platform path parser. The project domain decides what the string means;
  rewriting it would silently inspect a different directory than the one requested.
- **No DBC coupling in either direction.** The project surface does not import the DBC
  surface, and no DBC endpoint was changed. `DBC` is not a project validator and the project
  endpoint is not a DBC validator.

### 4.1 Error semantics

The domain errors are the existing ones; **no new taxonomy and no second mapping table was
created**. `ProjectError` already had an application-level handler
(`runtime/canx/api/app.py`) and a status rule (`status_for` in `runtime/canx/api/errors.py`
→ 400 for every project failure), so `runtime/canx/api/errors.py` needed **no change at
all** — the file was deliberately left untouched.

The codes this endpoint can therefore return, as produced by the domain:

```text
project.not_found                    project.manifest_missing
project.not_a_directory              project.manifest_malformed
project.manifest_unreadable          project.unsupported_format
project.unsupported_schema_version   project.database_missing
project.database_schema_invalid      project.database_unreadable
project.metadata_missing             project.metadata_invalid
project.identity_mismatch            project.close_failed
```

Every one is reported as the shared five-field envelope (`code` / `message` / `details` /
`recoverable` / `source = "project"`) with a **400**, never downgraded into a 500. Framework
request-shape validation keeps its own **422** (`api.request_validation_failed`,
`source = "api"`), and the two layers are never merged.

## 5. Changed files

```text
runtime/canx/api/project.py                              (new)  188 lines — the router + adapter
runtime/canx/api/app.py                                  (mod)  +1 import, +1 include_router, comment
tests/unit/api/test_project_api.py                       (new)  600 lines — 24 tests
tests/integration/test_project_api_integration.py        (new)  261 lines —  9 tests
tests/integration/test_packaged_runtime_smoke.py         (mod)  +108 lines —  1 packaged test
docs/PROJECT_STATE.md                                    (mod)  V0.3-11 status + capability row
docs/acceptance/v0.3-11-project-runtime-read-model-api-foundation.md   (new)  this report
```

Diff: 2 files modified (+113 / −1) plus 3 new files (1049 lines).

Deliberately untouched: `runtime/canx/project/**` (the domain), `runtime/canx/api/errors.py`,
`runtime/canx/api/dbc.py`, `runtime/canx/api/trace.py`, `runtime/canx/dbc/**`,
`apps/desktop/**`, the SQLite schema, and `project.json`. No architectural blocker was found
that would have required touching any of them.

## 6. RED → GREEN evidence

Three independent REDs, each of which turned the same green suite red, and each of which was
reverted before the commit.

### 6.1 Natural RED — the feature is absent

```text
$ .venv/Scripts/python.exe -m pytest tests/unit/api/test_project_api.py -q
ERROR collecting tests/unit/api/test_project_api.py
E   ModuleNotFoundError: No module named 'canx.api.project'
1 error in 0.51s
```

### 6.2 Behavioural RED — the endpoint is not mounted (404, not 200)

`app.include_router(create_project_router())` was temporarily removed from the application
factory, so the module existed but nothing served it:

```text
$ .venv/Scripts/python.exe -m pytest tests/unit/api/test_project_api.py -q
FAILED tests/unit/api/test_project_api.py::test_a_valid_project_answers_with_its_canonical_read_model
   >       assert response.status_code == 200, response.text
   E       AssertionError: {"detail":"Not Found"}
   E       assert 404 == 200
...
24 failed in 2.50s
```

Mutation reverted; the same 24 tests plus the 9 integration tests then passed
(§7). This is the RED the phase brief asked for: *the endpoint does not exist → 404 /
test failure*.

### 6.3 Behavioural RED — the project open runs on the event loop

The route was temporarily changed to call the blocking function directly
(`metadata, schema_version = _inspect_project(project_path)` instead of
`await asyncio.to_thread(...)`):

```text
$ .venv/Scripts/python.exe -m pytest tests/unit/api/test_project_api.py -q -k "off_the_event_loop"
FAILED tests/unit/api/test_project_api.py::test_a_complete_project_open_runs_off_the_event_loop
FAILED tests/unit/api/test_project_api.py::test_a_failing_project_open_also_runs_off_the_event_loop
  >       assert all(thread != event_loop_thread for thread in spy.threads)
  E       assert False
2 failed, 22 deselected in 1.30s
```

Mutation reverted (`md5sum runtime/canx/api/project.py` → `2bb1e85f52785ad99f05a163b298a481`,
identical to the pre-mutation file); the suite then passed (§7). No mutation remains in the
working tree: `grep -n "asyncio.to_thread" runtime/canx/api/project.py` reports the
`await asyncio.to_thread(_inspect_project, project_path)` call at line 132, and
`grep -n include_router runtime/canx/api/app.py` reports
`app.include_router(create_project_router())` at line 250.

## 7. Unit tests

```text
$ .venv/Scripts/python.exe -m pytest tests/unit/api/test_project_api.py -q
24 passed in 1.87s
```

The 24 cases, mapped to the phase brief's list:

| # | Behaviour pinned | Test |
| --- | --- | --- |
| 1–5 | valid project → 200, exact `project_id` / `display_name` / `schema_version` / `created_at` / `updated_at` | `test_a_valid_project_answers_with_its_canonical_read_model` |
| — | identity is never derived from the directory name | `test_the_identity_is_never_derived_from_the_directory_name` |
| 6 | response contains no filesystem path | `test_the_read_model_publishes_no_filesystem_location` |
| 7 | nonexistent project → `project.not_found` | `test_a_project_path_that_does_not_exist_is_not_found` |
| 8 | path is a file, not a directory → `project.not_a_directory` | `test_a_file_where_a_directory_belongs_is_not_a_directory` |
| 9 | missing manifest | `test_a_directory_without_a_manifest_reports_the_missing_manifest` |
| 10 | malformed manifest | `test_a_malformed_manifest_is_reported_as_malformed` |
| — | foreign manifest format | `test_a_manifest_of_another_format_is_refused` |
| 11 | unsupported manifest schema version | `test_an_unsupported_manifest_schema_version_is_refused` |
| 12 | missing `project.db` | `test_a_missing_database_is_reported_as_a_missing_database` |
| 13 | database/schema invalid | `test_a_database_missing_its_schema_tables_is_refused` |
| 14 | manifest/database identity mismatch | `test_a_manifest_that_disagrees_with_the_database_is_an_identity_mismatch` |
| 15 | shared five-field envelope, never 500 | `test_a_domain_failure_is_never_downgraded_to_a_server_error` (+ every failure case) |
| 16 | missing `project_path` → 422 `api.request_validation_failed` | `test_a_missing_project_path_is_a_request_validation_failure` |
| 17 | request boundary exists and is shared | the same test (location `query.project_path`, no `{"detail": …}`), plus `test_a_rejected_request_never_reaches_the_project_domain` |
| 18 | `ProjectService.open` runs off the event-loop thread | `test_a_complete_project_open_runs_off_the_event_loop`, `test_a_failing_project_open_also_runs_off_the_event_loop` |
| 19 | a successful request closes its handle | `test_the_success_path_closes_its_project_handle` |
| 20 | repeated requests retain no handle | `test_repeated_inspection_retains_no_handle` |

Additional cases beyond the brief: two projects never answer for each other; an inspection
leaves the project byte-identical; a failure *after* the open still closes the handle; a
response that cannot be built leaks no connection; the Runtime normalizes nothing about the
supplied path.

### 7.1 Event-loop boundary — how it is asserted

Being an `async` route says nothing about where the work happened, so the assertion is about
the *work*: `canx.api.project._open_project` is wrapped to record `threading.get_ident()`
**before** it runs, and every recorded thread must differ from the loop's thread. The record
happens before the call because a refused target never returns a handle — and validating a
refused target is exactly the expensive part that must not run on the loop.

This is a real bound, not a formality: `ProjectService.open` walks the filesystem, reads and
parses `project.json`, opens SQLite, reads metadata and verifies every table of the schema.

### 7.2 Handle ownership — how it is asserted

"Closed" is observed, not asserted from reading code: the spy keeps every
`ProjectHandle` the request opened and the tests assert `handle.closed is True` for each, and
that no handle survives its request. Three paths are covered separately:

```text
success                       → handle closed, exactly one open per request
failure after open            → read_metadata raises  → handle still closed (400 project.metadata_unreadable)
response construction failure → _inspect_payload raises → handle still closed, nothing left to leak
25 sequential requests        → 25 opens, 25 closes, 0 leaked
```

The response-construction case is the ordering proof: the projection is built *after* the
owned scope has exited, so cleaning up cannot depend on the response succeeding.

## 8. Integration tests

```text
$ .venv/Scripts/python.exe -m pytest tests/integration/test_project_api_integration.py -q
9 passed in 1.21s
```

Nothing is mocked. A real project is created by `ProjectService.create` on the real
filesystem, closed, and then reached over the real FastAPI ASGI application — which opens it
again and validates `project.json`, `project.db`, the schema and the agreement before it
answers. The expected values are read back *from disk* (`project.json` parsed, and the
`project_metadata` row queried through `sqlite3` directly) and compared against the HTTP
answer, so the endpoint cannot be a second opinion about what the project is.

Covered: the end-to-end create → close → inspect chain; a real missing-database rejection; a
real identity mismatch; a plain directory that is not a project (and is left untouched); a
foreign JSON manifest; two real projects never confused with each other; five consecutive
inspections leaving the project byte-identical; the answer being the same whether the creator
handle was still open or had been closed; an unsupported manifest schema.

## 9. Packaged runtime proof

The phase adds a Runtime endpoint, so `/health` is not evidence. The frozen executable itself
was asked.

```text
$ MSYS_NO_PATHCONV=1 cmd.exe /c "scripts\build-runtime.cmd"        → exit 0
  build\runtime-dist\canx-runtime.exe                              60,132,031 bytes

$ CANX_TEST_RUNTIME_EXE=<repo>\build\runtime-dist\canx-runtime.exe \
    .venv/Scripts/python.exe -m pytest tests\integration\test_packaged_runtime_smoke.py -q
6 passed in 27.86s
```

The new test, `test_packaged_runtime_serves_the_project_read_model_api`, launches
`canx-runtime.exe` and asserts, against the packaged process:

```text
create a temporary CAN-X project (source side) → close it
launch packaged canx-runtime.exe
GET  /project/inspect?project_path=…      → 200
     exact project_id, display_name, schema_version, created_at, updated_at
     the project path is absent from the body
GET  /project/inspect  (again)            → 200, identical body  (no retained state)
GET  /project/inspect?project_path=<absent> → 400 project.not_found, source = "project",
                                             shared five-field envelope, target not created
GET  /project/inspect  (no query at all)  → 422 api.request_validation_failed, source = "api"
POST /runtime/shutdown                    → 202, process exits 0
```

The `created_at` / `updated_at` the packaged process reports are compared against the values
the source tree reads from the same `project.db`, so the image cannot be answering from a
cached or re-derived value. `canx.api.project` is present in the image because the entry
point imports the application factory that mounts the router.

## 10. Full Python regression

```text
$ .venv/Scripts/python.exe -m pytest -q
1660 passed, 1 skipped in 165.18s (0:02:45)

$ .venv/Scripts/ruff.exe check runtime tests tools
All checks passed!

$ .venv/Scripts/mypy.exe runtime
Success: no issues found in 65 source files
```

Baseline before this phase was 1626 passed + 1 skipped; this phase adds 34 tests
(24 unit + 9 integration + 1 packaged).

The single skip is pre-existing and environmental:
`tests/unit/dbc/test_dbc_asset.py` cannot create a directory symlink on this Windows host
(`WinError 1314: 客户端没有所需的特权`) — unrelated to this phase, unchanged from V0.3-10.

## 11. Frontend regression

No frontend file was modified. The suites were still run.

```text
$ npm test          → Test Files 12 passed (12) · Tests 162 passed (162)
$ npm run lint      → exit 0   (eslint . --max-warnings 0)
$ npm run typecheck → exit 0   (tsc -b --pretty false)
$ npm run build     → exit 0   ✓ 692 modules transformed · built in 1.07s
```

Numbers identical to the V0.3-10 baseline — no frontend behaviour changed.

## 12. Rust regression

No Rust file was modified. The suites were still run.

```text
$ cargo fmt --check                                        → exit 0
$ cargo clippy --all-targets --all-features -- -D warnings → exit 0
$ cargo test                                               → exit 0
   28 passed; 0 failed   (lib unit tests)
    3 passed; 0 failed   (tests/runtime_sidecar.rs)
    0 failed everywhere
```

## 13. Windows packaging

```text
$ MSYS_NO_PATHCONV=1 cmd.exe /c "scripts\package-windows.cmd"     → exit 0
[1/6] building packaged Python runtime...        PyInstaller build complete
[2/6] verifying staged Tauri sidecar...          ok: canx-runtime-x86_64-pc-windows-msvc.exe
[3/6] running packaged-runtime smoke test...     (did not fail; the script stops here on failure)
[4/6] building Tauri MSI...                      Built application at: ...\target\release\can-x.exe
                                                 Finished 1 bundle:
                                                 ...\bundle\msi\CAN-X_0.1.0_x64_en-US.msi
[5/6] verifying MSI artifact...                  ok: CAN-X_0.1.0_x64_en-US.msi
[6/6] packaging complete.
```

Artifacts on this host:

```text
build\runtime-dist\canx-runtime.exe                              60,130,837 bytes
apps\desktop\src-tauri\binaries\canx-runtime-x86_64-pc-windows-msvc.exe
apps\desktop\src-tauri\target\release\bundle\msi\CAN-X_0.1.0_x64_en-US.msi   63,139,840 bytes
```

Honest note on step 3: the packaging script streams its smoke output, and the invocation above
was captured through a `tail`, so the step's own line count was not retained. The script
fails fast at the first error and steps 4–6 ran, which means step 3 did not fail; the same
command run separately against the same executable reports **6 passed in 27.86s** (§9). This
proves the MSI **builds** on this host; it does not prove a windowed run renders anything new
— nothing about the desktop changed.

## 14. Schema / dependency changes

```text
SQLite migration                        0
project.json change                     0
project.db schema change                0
ProjectMetadata change                  0
Parquet change                          0
DBC canonical model change              0
new Python dependency                   0
new npm dependency                      0
new Rust dependency                     0
Runtime HTTP endpoints                  +1   (GET /project/inspect)
Runtime HTTP endpoints removed           0
```

The endpoint is an adapter over an existing domain. No domain model had to be changed, and
`runtime/canx/api/errors.py` needed no new registration — the `ProjectError` handler and the
400 status rule were already in place.

**Legacy reuse: none.** No CAN-Space / CanLab code entered CAN-X; `docs/REUSE_LEDGER.md`
remains not created.

## 15. Known limitations and NOT VERIFIED items

- **`GET` query-string shape validation is limited to presence and type.** A repeated
  `project_path` query parameter is resolved by the framework to a single value rather than
  rejected; this matches every other endpoint in this application and was **not** changed
  here. Only a missing parameter produces the 422 request-validation envelope. This is
  documented rather than asserted, because asserting framework behaviour this phase did not
  choose would be pinning someone else's decision.
- **An empty `project_path` is passed to the domain unmodified.** The domain then answers for
  `.` — its own decision about that string. The phase deliberately does not "fix" this by
  normalizing or rejecting in the adapter, because the brief forbids the adapter from
  interpreting a path. The behaviour is pinned by a test so a future change is a visible
  decision, not a drift.
- **No Desktop caller exists yet.** This endpoint has no production consumer: the Desktop
  project picker is a later increment. Nothing in `apps/desktop` was touched.
- **The packaged endpoint is proven over HTTP, not in a window.** No desktop UI change was
  made, so there is nothing new to render; a windowed run was not performed.
- Pre-existing items still open and unchanged by this phase: real CAN hardware NOT VERIFIED;
  macOS NOT VERIFIED; windowed desktop launch NOT VERIFIED; realtime/UI latency PARTIAL;
  10–50 GB dataset NOT VERIFIED; near-limit Desktop HTTP serialization / renderer starvation
  (P2, unresolved); timestamp segment pruning still disabled; DBC registry-vs-filesystem
  non-atomicity; `dbc/`-as-symlink rejection skipped on Windows.
- No CI exists (`.github/` is absent), so every number above is a **local** run, not a CI
  result.

## 16. Architecture gates

```text
Project domain remains Runtime authority?             YES
API directly parses project.json?                     NO
API directly queries SQLite metadata manually?        NO   (through ProjectHandle.read_metadata)
ProjectService reused?                                YES
ProjectHandle always closed?                          YES  (with-statement + three failure paths tested)
Blocking project open runs off event loop?            YES  (asserted by thread identity)
Global current project introduced?                    NO
Persistent active ProjectHandle introduced?           NO
Desktop Project Picker introduced?                    NO
DBC used as Project validator?                        NO
New SQLite migration?                                 NO
New dependency?                                       NO
Packaged runtime endpoint verified?                   YES
```

## 17. Independent acceptance

Nothing in this report is a verdict. The development agent did **not** write
`Final Acceptance: PASS` and did **not** mark the phase `CLOSED`.
`docs/PROJECT_STATE.md` records this phase only as *implementation complete · local
verification complete · awaiting independent acceptance*, and no subsequent phase has been
started. V0.3-12 was not entered.

**Acceptance state: Awaiting independent acceptance.**
