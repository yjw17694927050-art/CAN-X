# V0.4-00 — Desktop Development Preflight & Dev-Port Closure

> **Task**: V0.4-00 · **Owner**: Main Agent
> **Branch**: `maintenance/v0.4-00-desktop-dev-preflight`
> **Base SHA**: `127ee2ff3ba0600afc3c7826e879981ef0a64867` (V0_4_WAVE1_BASE_SHA)
> **Status**: IMPLEMENTED · SELF-VERIFIED · READY FOR INDEPENDENT ACCEPTANCE
>
> The development side never writes `Final Acceptance: PASS` / `Status: CLOSED`
> (`AGENTS.md` §14, `docs/PROJECT_STATE.md` §11). Those are the independent
> reviewer's verdict.

---

## 1. The defect

The V0.3 baseline shipped a P2 `ACCEPTED NON-BLOCKING` defect:

```text
apps/desktop/src-tauri/tauri.conf.json   build.devUrl = "http://localhost:1420"
apps/desktop/src-tauri/tauri.conf.json   build.beforeDevCommand = "pnpm dev"
apps/desktop/vite.config.ts              (no server configuration at all)
```

The Tauri dev shell polls exactly the URL its `devUrl` names — `localhost:1420`.
But `apps/desktop/vite.config.ts` pinned no port, so Vite bound whatever free
port it found (1420, 1421, …). When it did **not** land on 1420, `pnpm tauri dev`
waited forever on a server it could not reach; and even when it did land on 1420,
a taken port silently pushed Vite to 1421 while Tauri kept polling 1420. The two
configs were free to drift, and nothing detected it.

## 2. The fix (minimal, fail-closed)

`apps/desktop/vite.config.ts` now declares the dev-port contract explicitly:

```ts
server: {
  port: 1420,
  strictPort: true,
},
```

- `port: 1420` makes the Vite dev server bind the exact port Tauri waits on.
- `strictPort: true` makes it **fail closed**: if 1420 is taken, Vite refuses to
  start rather than silently moving to another port — so a misconfiguration
  surfaces as an error instead of as a hung dev shell.

No product architecture, Runtime API, recorder, Plot, Replay, dependency,
lockfile or dev-server-proxy change was made. `beforeDevCommand` and `devUrl`
in `tauri.conf.json` were left byte-identical.

## 3. Regression guard

`apps/desktop/src/smoke/dev-port-contract.test.ts` locks the contract so the two
configs can never drift again. It reads the **real** configs, not source text:

- the Vite config as the module Vite itself loads (`import viteConfig from
  "../../vite.config"`), so a comment that merely mentions `1420` cannot satisfy
  it; and
- the Tauri config as parsed JSON, re-deriving the port with the WHATWG `URL`
  parser rather than asserting a literal string.

It asserts three things:

```text
Tauri devUrl port          == 1420
Vite server.port           == Tauri devUrl port
Vite server.strictPort     === true
```

## 4. RED → GREEN

The guard was written to fail against the pre-fix configuration, and the fix was
then restored. The RED run used the original `vite.config.ts` (checked out from
the base with `git show`, the working fix preserved aside and restored after):

```text
RED   (original vite.config.ts, no server block)  2 failed | 1 passed
      expect(viteConfig.server?.port).toBe(1420)          -> undefined
      expect(viteConfig.server?.strictPort).toBe(true)    -> undefined
GREEN (port: 1420, strictPort: true)              3 passed
```

The third case (the Tauri `devUrl` is 1420) passes in both runs, which is
correct: that half of the contract was never the broken one.

## 5. Verification

| Gate | Command | Result |
| --- | --- | --- |
| focused guard | `vitest run src/smoke/dev-port-contract.test.ts` | **3 passed** |
| frontend lint | `eslint . --max-warnings 0` | exit 0 |
| frontend typecheck | `tsc -b --pretty false` | exit 0 |
| frontend test | `vitest run` | **34 files / 536 tests passed** |
| frontend build | `tsc -b && vite build` | exit 0 |

The full frontend suite rose from the V0.3 baseline's 33 files / 533 tests to 34
files / 536 tests — exactly the new guard and its three cases, no other change.

## 6. Real native `pnpm tauri dev` smoke — VERIFIED

V0.4-00-FIX-1 closed the one blocker V0.4-00 carried: the real dev flow had never
been run. It has now been run on this tree and all fourteen required observations
were captured. This is the genuine flow — Tauri itself ran `beforeDevCommand`;
the Vite server was **not** started by hand.

Environment note: `pnpm` is not installed on this machine and `corepack` is
absent (checked on `PATH`, `%APPDATA%\npm`, `%LOCALAPPDATA%\pnpm`,
`%PROGRAMFILES%\nodejs` and the bundled node runtime). `pnpm@11.19.0` — the exact
version `package.json` pins — was obtained through `npx` and run from
`apps/desktop`; no global install was performed, and Tauri's own
`beforeDevCommand` (`pnpm dev`) resolved the same `pnpm` on the augmented `PATH`.

```text
$ npx --yes pnpm@11.19.0 tauri dev            (cwd apps/desktop)
$ tauri "dev"
     Running BeforeDevCommand (`pnpm dev`)              <- Tauri ran it, not a human
$ vite
  VITE v8.3.0  ready in 1496 ms
  Local:   http://localhost:1420/                       <- binds the declared port
     Running DevCommand (`cargo run ...`)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 29.67s
     Running `target/debug/can-x.exe`                   <- Desktop process
```

| # | Item | Observed evidence |
| --- | --- | --- |
| 1 | `pnpm tauri dev` starts | process `pnpm.mjs tauri dev` |
| 2 | beforeDevCommand launches Vite | log `Running BeforeDevCommand (pnpm dev)`; child `pnpm.mjs dev` -> `vite/bin/vite.js` |
| 3 | Vite binds 1420 | log `Local: http://localhost:1420/`; `GET :1420/` -> HTTP 200 with `id="root"` |
| 4 | Tauri connects to devUrl 1420 | the dev flow proceeded to build and run the app |
| 5 | Desktop process starts | `can-x.exe` (pid 16324, `target/debug/can-x.exe`) |
| 6 | CAN-X window created | `EnumWindows` -> class `Tauri Window`, title `CAN-X`, `IsWindowVisible=True` (hwnd 6818002) |
| 7 | WebView loads frontend | Vite served HTTP 200 (`id="root"`, 598 B) to that devUrl |
| 8 | Runtime sidecar spawned by Desktop | pid 18176 `python.exe -m canx --host 127.0.0.1 --port 8765 --session-token ...` |
| 9 | `/health` reachable | HTTP 200 `{"status":"ready","service":"canx-runtime","schema_version":1}` |
| 10 | `/runtime/status` reachable | HTTP 200 `{"state":"ready","capture_active":true,"capture_state":"running","failure":null}` |
| 11 | Desktop shutdown completes | `WM_CLOSE` to the CAN-X window -> `can-x.exe` gone |
| 12 | Runtime sidecar terminates | `/health` unreachable; no listener on port 8765 |
| 13 | no orphan canx-runtime | no `canx-runtime.exe` / `-m canx --host` process; 8765 listener NONE |
| 14 | no orphan dev process | `vite` gone, `tauri` CLI gone; no listener on 8765 or 1420 |

No product code was changed. The smoke found no defect, so per the brief the fix
stays exactly as V0.4-00 shipped it and only this evidence moved from
NOT VERIFIED to VERIFIED.

## 7. Ownership

Modified files (all inside the V0.4-00 ALLOWED surface):

```text
apps/desktop/vite.config.ts
apps/desktop/src/smoke/dev-port-contract.test.ts   (new)
docs/acceptance/v0.4-00-development-preflight.md   (new)
```

No file outside the surface was touched. `apps/desktop/src-tauri/**`,
`runtime/**`, `.github/**`, the protected manifests and the authority documents
are byte-identical to the base.

## 8. Known limitations

- The native smoke resolves `pnpm` through `npx pnpm@11.19.0` because this machine
  has neither an installed `pnpm` nor `corepack`. A CI runner or workstation that
  DOES have `pnpm` on `PATH` should run the identical flow with `pnpm tauri dev`;
  the §6 evidence does not depend on how `pnpm` was resolved.
- A second dev-port consumer (e.g. a future preview server) is out of scope;
  the guard covers only the `tauri.conf.json` `devUrl` ↔ `vite.config.ts`
  `server.port` contract.
