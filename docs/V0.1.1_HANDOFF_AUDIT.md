# CAN-X V0.1.1 — Handoff / Takeover Audit

> **Date**: 2026-09-16
> **Auditor**: 天枢 harness (taking over from Codex)
> **Basis**: current working tree + current test runs + current git diff. **Not** derived from prior reports.
> **Environment**: Windows 11; `py` → Python 3.13.15; node 24.18.0; cargo 1.98.1; pnpm 11.19.0 (via `npx`). Virtual CAN only; no physical hardware, no macOS.

---

## 0. Repository state on arrival, and what was done to preserve it

Two independent lines of uncommitted/unpushed work existed:

1. **In `main` (working tree, uncommitted):** a relocation of `AGENTS.md`, `PRD.md`, `README.md`, `SPEC.md` into `docs/`, plus a new `docs/PROJECT_STATE.md`. It did **not** carry Codex's filename fix.
2. **On branch `codex/v0.1.1-acceptance-hardening`** (worktree `.worktrees/v0.1.1-acceptance-hardening`, HEAD `f57e09b`, 8 commits over `a8260fc`): the recorder-pressure hardening, filename fix, new ADR/plan/spec docs, and test changes.

Actions taken (all non-destructive):

| Step | Result |
|---|---|
| Committed the pre-existing `docs/` relocation | `ebb0ac0` — preserved, no content changed |
| Merged `codex/v0.1.1-acceptance-hardening` into `main` | `8ee94f5` — clean; git rename-detection applied Codex's `AGENTS.md` fix onto `docs/AGENTS.md` |
| Re-ran suites on integrated `main` | Python **106 passed**; frontend **17 passed** |

Nothing was reset, cleaned, checked-out-over, or deleted. Codex's 8 commits are in `main`'s history.

**Layout discrepancy (flagged, not changed):** the post-merge tree keeps the pre-existing relocation, so the canonical guides now live at `docs/AGENTS.md`, `docs/PRD.md`, `docs/SPEC.md`, and no root `AGENTS.md` exists. `docs/PROJECT_STATE.md` §3 and `docs/SPEC.md` §3 still describe these as root-level paths. Because `AGENTS.md` at repository root is the agent-tooling convention, the project owner should confirm the intended layout.

---

## A. AGENTS.md document naming consistency

- **Status:** DONE
- **Evidence:** commits `7a77b2d` ("docs: standardize AGENTS guidance filename") and `a0b6831`. Independent scan `grep -rn "AGENT\.md" docs/ *.md` → **NONE FOUND**. `docs/AGENTS.md:3` now reads `` `AGENTS.md` ``.
- **Files:** `docs/AGENTS.md`, `docs/PRD.md`, `docs/SPEC.md`, `docs/V0.1_TECH_VALIDATION.md`
- **Tests:** negative source scan for the legacy singular filename passes (no output, exit 0).
- **Remaining:** none.

## B. Recorder Backpressure P0

- **Status:** DONE
- **Evidence:** `docs/ADR/0001-recorder-pressure-policy.md` (Status: Accepted). Commits `2e01986` (bound lossless subscriber pressure), `4cd55de` (coordinate capture shutdown phases), `1607239` (expose degraded recording sessions), `c1df7f5` (reset recorder state for unrecorded sessions). Code: `runtime/canx/capture/subscriber.py` (finite `publish_timeout_seconds`, `disable()`), `runtime/canx/capture/pipeline.py` (deadline + concurrent fan-out), `runtime/canx/runtime/service.py` (degraded session state, bounded archive cleanup). Recorder failure no longer blocks capture; capture continues degraded.
- **Files:** `runtime/canx/capture/subscriber.py`, `runtime/canx/capture/pipeline.py`, `runtime/canx/recorder/msgpack_recorder.py`, `runtime/canx/runtime/service.py`, `runtime/canx/api/app.py`, `runtime/canx/metrics/*`
- **Tests:** `tests/unit/capture/test_pipeline.py` (22 tests), `tests/integration/test_recorder_independence.py`, `tests/integration/test_runtime_capture.py`, `tests/unit/api/test_runtime_lifecycle.py`.
- **Remaining:** real slow-disk / long-duration soak (see D).

## C. Recorder saturation test

- **Status:** DONE
- **Evidence:** commit `f57e09b` ("test: prove recorder saturation behavior"). `tests/integration/test_recorder_independence.py::test_archive_saturation_degrades_and_stops_without_releasing_recorder` drives a permanently-blocked recorder to real saturation (queue peak 4, 6 uncommitted frames, stream still advances). Reproduced in the 106-pass run.
- **Files:** `tests/integration/test_recorder_independence.py`, `tests/integration/test_runtime_capture.py`
- **Tests:** present and passing.
- **Remaining:** none.

## D. Recorder soak / failure tests

- **Status:** PARTIAL
- **Evidence:** failure-path tests exist — `tests/unit/recorder/test_msgpack_recorder.py` (write error → `FAILED`, missing-dir `OSError`), capacity/deadline validation, bounded stop. But there is **no long-duration or slow-but-progressing-disk soak**. `docs/ADR/0001-recorder-pressure-policy.md` §Consequences and the Task-4 report both defer it explicitly: *"Long-duration disk behavior still requires real-device soak testing; the V0.1.1 synthetic suite proves state and lifecycle semantics only"* and *"does not establish a hard wall-clock bound … for an indefinitely hung OS disk thread"*.
- **Files:** `tests/integration/test_recorder_independence.py`, `runtime/canx/runtime/service.py`
- **Tests:** saturation + failure covered; soak not covered.
- **Remaining:** add a soak test (sustained capture under a slow *but progressing* recorder) proving bounded memory/queue, no silent loss, and correct counters over a longer run. ← **first incomplete task**

## E. Shared frontend realtime stream

- **Status:** NOT STARTED
- **Evidence:** `apps/desktop/src/components/trace/LiveTracePanel.tsx:23` and `apps/desktop/src/components/plot/LivePlotPanel.tsx:27` each construct their own `new WebSocket("ws://127.0.0.1:8765/stream/frames")` and their own `new Worker(…/frame-worker.ts)`. Trace and Plot run two independent pipelines; no shared store. Confirmed as the next planned optimization in `docs/V0.1_VALIDATION_REPORT.md`.
- **Files:** `apps/desktop/src/components/trace/LiveTracePanel.tsx`, `apps/desktop/src/components/plot/LivePlotPanel.tsx`, `apps/desktop/src/workers/*`, `apps/desktop/src/runtime/*`
- **Tests:** none for a shared stream.
- **Remaining:** one workspace-owned provider → single WebSocket + single Worker + bounded store; Trace/Plot subscribe to projections.

## F. Frontend regression tests

- **Status:** NOT STARTED
- **Evidence:** only V0.1 tests exist (`capture-client.test.ts`, `frame-worker.test.ts`, `TracePanel.test.tsx`, `series.test.ts`, `App.test.tsx` = 17 tests). No regression tests for the shared-stream path (depends on E).
- **Files:** `apps/desktop/src/**/*.test.ts(x)`
- **Tests:** current suite green (17), but does not cover V0.1.1 behavior.
- **Remaining:** add regression tests alongside E.

## G. Packaged Python runtime proof

- **Status:** NOT STARTED
- **Evidence:** `apps/desktop/src-tauri/tauri.conf.json` has `bundle.active=false` and no `externalBin`; `runtime_sidecar.rs:103` `RuntimeSidecar::spawn(python: &Path, …)` takes a caller-supplied interpreter path (dev/system python). No packaged `canx-runtime.exe` and no production resolution path.
- **Files:** `apps/desktop/src-tauri/tauri.conf.json`, `apps/desktop/src-tauri/src/runtime_sidecar.rs`, `apps/desktop/src-tauri/src/lib.rs`
- **Tests:** none.
- **Remaining:** PyInstaller-style packaged runtime + production-only resolution with no system-python fallback (design stage 3).

## H. Tauri distribution proof

- **Status:** NOT STARTED
- **Evidence:** `apps/desktop/src-tauri/tauri.conf.json` → `"bundle": { "active": false, "icon": [] }`. No resources, no installer config.
- **Files:** `apps/desktop/src-tauri/tauri.conf.json`
- **Tests:** none.
- **Remaining:** enable a minimal Windows bundle and prove it builds.

## I. Desktop smoke test

- **Status:** NOT STARTED
- **Evidence:** `scripts/` contains only `rust-check.cmd` and `tauri-build.cmd`; no smoke script. `docs/V0.1_VALIDATION_REPORT.md` records desktop launch as **NOT VERIFIED**.
- **Files:** `scripts/`
- **Tests:** none.
- **Remaining:** launch distribution-owned runtime → `/health` → start capture → observe stream → stop/flush → exit, proving child exit.

## J. Realtime / UI latency instrumentation

- **Status:** NOT STARTED
- **Evidence:** `runtime/canx/benchmarks/v01_pipeline.py:142-152` hardcodes `stream_latency_ms`, `trace_render_latency_ms`, `plot_update_latency_ms`, `desktop_cpu_percent` to `None` / `"NOT VERIFIED"`.
- **Files:** `runtime/canx/benchmarks/v01_pipeline.py`, `apps/desktop/src/workers/*`
- **Tests:** `tests/performance/test_benchmark_contract.py` asserts these stay `None` today.
- **Remaining:** real monotonic measurements at runtime→WS, worker decode/publish, Trace/Plot update.

## K. Full regression

- **Status:** NOT STARTED (as the V0.1.1 gate)
- **Evidence:** current green baseline on integrated `main`: Python **106 passed** (`.venv/Scripts/python.exe -m pytest`), frontend **17 passed** (`pnpm test`). No full V0.1.1 matrix that includes the not-yet-written E/F/G tests; Rust/desktop suites not executed this session.
- **Files:** `tests/**`, `apps/desktop/src/**/*.test.ts(x)`
- **Tests:** baseline only.
- **Remaining:** run the complete Python + frontend + Rust matrix after E–J land.

## L. V0.1.1 Acceptance Report

- **Status:** NOT STARTED
- **Evidence:** `docs/V0.1.1_ACCEPTANCE_REPORT.md` does not exist.
- **Files:** —
- **Tests:** —
- **Remaining:** write it after the remaining stages, using only executed evidence.

## M. PROJECT_STATE update

- **Status:** PARTIAL
- **Evidence:** `docs/PROJECT_STATE.md` exists and states "V0.1 completed → V0.1.1 Acceptance Hardening pending" (§25 lists Recorder backpressure as an **open** P0). It predates the recorder-hardening merge and still lists the guides as root-level paths.
- **Files:** `docs/PROJECT_STATE.md`
- **Tests:** n/a (document).
- **Remaining:** update after V0.1.1 acceptance to reflect the recorder hardening, the shared stream, packaging, and the final Go/No-Go.

---

## Summary

| Task | Status |
|---|---|
| A. AGENTS.md naming consistency | DONE |
| B. Recorder backpressure P0 | DONE |
| C. Recorder saturation test | DONE |
| D. Recorder soak / failure tests | **PARTIAL** |
| E. Shared frontend realtime stream | NOT STARTED |
| F. Frontend regression tests | NOT STARTED |
| G. Packaged Python runtime proof | NOT STARTED |
| H. Tauri distribution proof | NOT STARTED |
| I. Desktop smoke test | NOT STARTED |
| J. Realtime/UI latency instrumentation | NOT STARTED |
| K. Full regression | NOT STARTED |
| L. V0.1.1 Acceptance report | NOT STARTED |
| M. PROJECT_STATE update | PARTIAL |

**First incomplete task: D — Recorder soak / failure tests.**
