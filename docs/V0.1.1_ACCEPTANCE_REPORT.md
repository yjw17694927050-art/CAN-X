# CAN-X V0.1.1 Acceptance Report

> **Date**: 2026-09-16
> **Host**: Windows 11 build 26200
> **Environment**: `py` → Python 3.13.15 · node 24.18.0 · pnpm 11.19.0 · cargo 1.98.1 · PyInstaller 6.17.0
> **Scope**: V0.1.1 Acceptance Hardening. Virtual CAN only; no physical hardware; no macOS host.
> **Basis**: commands executed in this environment this session. Nothing below is carried over from a prior report.

---

## Conclusion

**V0.1.1 acceptance hardening: implemented and verified within the headless Virtual-CAN boundary.**

Recommendation: **Conditional Pass / Go** to independent acceptance. All V0.1.1 goals are implemented and covered by executed evidence, with three items explicitly limited by the host (windowed desktop launch, UI/runtime latency, physical hardware/macOS). This remains a **No-Go** for production release, physical CAN claims, macOS claims, or performance thresholds.

---

## Takeover and preservation

On arrival the V0.1.1 work was split across two places: an uncommitted relocation of the top-level guides into `docs/` (plus `docs/PROJECT_STATE.md`) in `main`, and Codex's recorder-hardening work on branch `codex/v0.1.1-acceptance-hardening` (8 commits) in `.worktrees/v0.1.1-acceptance-hardening`.

Nothing was reset, cleaned, checked-out-over, or deleted. The pre-existing relocation was committed (`ebb0ac0`) and Codex's branch was merged (`8ee94f5`); git rename-detection applied Codex's `AGENTS.md` filename fix onto `docs/AGENTS.md`. The audit is in `docs/V0.1.1_HANDOFF_AUDIT.md`.

---

## Implemented across V0.1.1

- **A — documentation naming**: singular `AGENT.md` references replaced with canonical `AGENTS.md`; negative scan `grep 'AGENT\.md'` returns nothing.
- **B/C/D — recorder pressure**: ADR 0001 policy implemented — capture continues after a recorder pressure/write failure while the session becomes `degraded`; finite archive deadline, structured observable failure, bounded stop. Saturation test plus a new slow-disk soak test.
- **E/F — shared frontend realtime stream**: one workspace-owned store (`realtime-stream.ts`) holds a single WebSocket + single Worker; Trace and Plot are projections of it. Trace freeze is a Trace-only cursor and does not pause the socket/Worker/Plot. Consumers are reference-counted; the socket and worker open once and close once.
- **G — packaged Python runtime**: PyInstaller (`packaging/canx-runtime.spec`, `scripts/build-runtime.cmd`) produces `canx-runtime.exe`; the desktop resolves the bundled executable in production (`#[cfg(not(debug_assertions))]`) and never falls back to a system Python, keeping `CANX_RUNTIME_PYTHON`/repo `.venv` for development.
- **H — Tauri distribution**: bundle enabled with icons and the sidecar as an `externalBin`; `scripts/tauri-bundle.cmd` produces an MSI whose WiX manifest ships `canx-runtime.exe`.
- **I — smoke path**: `tests/integration/test_packaged_runtime_smoke.py` drives the packaged `canx-runtime.exe` through health → capture → binary stream → flush → authenticated shutdown → exit 0.
- **J — instrumentation**: the Worker records a monotonic per-batch decode duration, surfaced on the shared store and shown in the workspace status.
- **K/L/M — regression, report, state**: full matrix green; this report; `docs/PROJECT_STATE.md` updated.

---

## Verification

| Area | Command | Result |
|---|---|---|
| Python tests | `.venv\Scripts\python.exe -m pytest` | **108 passed** |
| Python lint | `.venv\Scripts\ruff.exe check runtime tests tools` | passed |
| Python types | `.venv\Scripts\mypy.exe runtime` | passed, 29 source files |
| Frontend tests | `pnpm test` | **26 passed** (7 files) |
| Frontend lint | `pnpm lint` | passed (`--max-warnings 0`) |
| Frontend types | `pnpm typecheck` | passed |
| Frontend build | `pnpm build` | passed (689 modules) |
| Rust | `scripts\rust-check.cmd` | fmt/check/Clippy passed; **3 tests passed** |
| Rust release path | `cargo check --release` | passed (validates the production-only resolver) |
| Packaged runtime | `scripts\build-runtime.cmd` | `canx-runtime.exe` (17.2 MB) built |
| Packaged runtime serve | spawn + `GET /health` | `{"status":"ready","service":"canx-runtime","schema_version":1}` |
| Desktop smoke | `pytest tests/integration/test_packaged_runtime_smoke.py` | **1 passed** |
| Desktop distribution | `scripts\tauri-bundle.cmd` | exit 0; `CAN-X_0.1.0_x64_en-US.msi` (20.2 MB) |
| Rendering | vite dev server + screenshot | workspace renders; stream state "Stream disconnected" shown |

The MSI's generated WiX manifest (`target\release\wix\x64\main.wxs`) references `canx-runtime.exe`, and the MSI is 20.2 MB against a 9.1 MB app binary — confirming the packaged sidecar ships.

---

## Per-goal status

| Goal | Status | Evidence |
|---|---|---|
| Recorder backpressure P0 | DONE | ADR 0001; `tests/unit/capture/test_pipeline.py` (22), `test_recorder_independence.py` saturation |
| Recorder saturation test | DONE | `test_archive_saturation_degrades_and_stops_without_releasing_recorder` |
| Recorder soak test | DONE | `test_slow_disk_soak_stays_lossless_and_bounded` (mutation-checked: fails if the append path is disabled) |
| Shared frontend stream | DONE | `realtime-stream.test.ts`, `LiveRealtimePanels.test.tsx` (one socket/worker; Trace-only freeze) |
| Frontend regression tests | DONE | 9 new tests (6 store + 2 panels + 1 worker decode) |
| Packaged Python runtime | DONE | `canx-runtime.exe` + `/health` proof + release-only resolution |
| Tauri distribution | DONE | MSI built; sidecar in the WiX manifest |
| Desktop smoke test | PARTIAL | runtime half verified end-to-end; windowed launch **NOT VERIFIED** |
| Realtime/UI latency | PARTIAL | worker decode measured; runtime→WS and Trace/Plot update latency **NOT VERIFIED** |
| Full regression | DONE | table above |
| Acceptance report | DONE | this document |
| PROJECT_STATE update | DONE | `docs/PROJECT_STATE.md` §25/§26/§32 and header |

---

## Known limitations and NOT VERIFIED

- **Windowed desktop launch** cannot run under the host execution policy; only the runtime half of the smoke path is verified. GUI launch is **NOT VERIFIED**.
- **Latency**: only Worker decode duration is instrumented. Runtime→WebSocket latency, Trace render latency, and Plot update latency remain **NOT VERIFIED**; the benchmark placeholders in `runtime/canx/benchmarks/v01_pipeline.py` still report `NOT VERIFIED`.
- **NSIS** bundling failed on a WiX→NSIS download timeout, so `bundle.targets` is restricted to `msi`. MSI is the verified Windows artifact.
- **PyInstaller onefile** spawns a child process; a forced kill of the bootloader alone can leave the child running. The supported path is the authenticated `/runtime/shutdown`, which exits cleanly (verified).
- **Physical CAN hardware**, error frames, hardware timestamps, bus-off, and TX policy remain **NOT VERIFIED**. **macOS** remains **NOT VERIFIED**.
- No CAN-Space/CanLab code is reused, so `docs/REUSE_LEDGER.md` is not created.

---

## Go / No-Go

- **Go:** hand V0.1.1 to independent acceptance, then proceed toward V0.2.
- **No-Go:** production use, real CAN TX, ECU mutation, hardware or macOS compatibility statements, or a Rust migration of Python business logic.

---

# Final Cleanup (V0.1.1-FINAL-CLEANUP)

Added 2026-09-16 as the closing increment. It does not alter the record above.

- **Root source-of-truth documents restored.** `AGENTS.md`, `PRD.md`, `SPEC.md`, `README.md` live at the repository root again, and the `docs/` duplicates were removed — exactly one source of truth per document. `pyproject.toml` `readme = "README.md"` resolves to the root file. `AGENTS.md` §2 now reads `1. AGENTS.md / 2. PRD.md / 3. SPEC.md / 4. docs/PROJECT_STATE.md（需要项目当前状态时）`. A full scan finds no `AGENT.md` (singular) and no `docs/AGENTS.md`, `docs/PRD.md`, `docs/SPEC.md`, or `docs/README.md` source-of-truth pointer outside this report's own history.
- **Shared stream partial-init resource leak fixed.** `RealtimeStreamStore.#start()` is now atomic: when the Worker or the WebSocket fails to construct, the resource that did construct is released (`Worker.terminate()` / `WebSocket.close()`) before the store reports `unsupported`. No orphan Worker or WebSocket remains.
- **Regression tests added.** `apps/desktop/src/runtime/realtime-stream.test.ts` gained `terminates a created worker when the socket cannot be created` and `closes a created socket when the worker cannot be created`. Both were RED against the previous code (the created resource was not released) and are GREEN after the fix. The pre-existing `both unavailable` test is retained.
- **Clean Windows packaging entry added.** `scripts/package-windows.cmd` is the single entry: build runtime → verify staged sidecar → packaged-runtime smoke → Tauri MSI → verify `*.msi` → exit code, failing fast at the first error. It rebuilds the whole chain from a clean checkout and never requires a hand-run `build-runtime.cmd` first.
- **Clean packaging result.** `scripts\package-windows.cmd` ran end to end and exited 0: sidecar staged, smoke `1 passed`, `CAN-X_0.1.0_x64_en-US.msi` built and verified.

## Final regression

| Area | Command | Result |
|---|---|---|
| Python tests | `.venv\Scripts\python.exe -m pytest` | **108 passed** |
| Python lint | `.venv\Scripts\ruff.exe check runtime tests tools` | passed |
| Python types | `.venv\Scripts\mypy.exe runtime` | passed, 29 files |
| Frontend tests | `pnpm test` | **28 passed** (7 files) |
| Frontend lint | `pnpm lint` | passed |
| Frontend types | `pnpm typecheck` | passed |
| Frontend build | `pnpm build` | passed |
| Rust | `scripts\rust-check.cmd` | fmt/Clippy passed; **3 tests passed** |
| Windows packaging | `scripts\package-windows.cmd` | **exit 0** — runtime + smoke + MSI verified |

## Remaining NOT VERIFIED

Unchanged from the section above: windowed desktop launch; runtime→WebSocket, Trace, and Plot update latency; physical CAN hardware; macOS.
